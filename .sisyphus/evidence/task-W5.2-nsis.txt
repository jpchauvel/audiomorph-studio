Task: W5.2 — Windows installer (NSIS x64 + CUDA detection + optional signing)
Date: 2026-05-16
Status: COMPLETE

Files created/modified:
- apps/shell/electron-builder.yml: expanded win section (icon, publisherName, verifyUpdateCodeSignature) and nsis section (createDesktopShortcut, createStartMenuShortcut, installerIcon, uninstallerIcon, include)
- apps/shell/build/installer.nsh: NSIS customInit macro running PowerShell Get-CimInstance for NVIDIA GPU detection with MessageBox prompt (Yes/No → Abort)
- scripts/build-win.sh: executable, runs `bun run build:all && electron-builder --win --x64`; uses WIN_CSC_LINK + WIN_CSC_KEY_PASSWORD if set (optional, not required)
- apps/shell/tests/packaging/win.test.ts: 6 Vitest tests covering yml config, installer.nsh content (Get-CimInstance, NOT wmic), build script existence/syntax/perms

Verification:
- npx vitest run (apps/shell): 14 files, 80 passed (previous 74 + 6 new)
- bash -n scripts/build-win.sh: OK
- chmod +x verified

Constraints honored:
- No wmic (Get-CimInstance only)
- perMachine: false (no UAC complexity)
- WIN_CSC vars optional
- No mac section changes
- Vitest (not bun test)
